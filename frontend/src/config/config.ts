const config = {
  API_HOST: process.env.NEXT_PUBLIC_API_URL,
  LOCAL_STORAGE: {
    TOKEN: "token",
    USER_NAME: "username",
    CART: {
      PRODUCTS: "products",
    },
  },
};

export default config;
